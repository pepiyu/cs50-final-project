import Navbar from '../Navbar';
import Timer from '../Timer';

const Desktop = () => {
    return (
        <div className="desktop">
            <Navbar/>
            <Timer/>

        </div>
    );
}

export default Desktop;